# Excel Merger - Универсальное объединение Excel файлов

Desktop приложение на Go для объединения нескольких Excel документов с идентичной структурой в один файл.

## 📋 Описание

Excel Merger использует принцип **шаблонного объединения**: вы выбираете базовый файл, настраиваете правила для каждого листа (номер строки с заголовками, включение/выключение), и все остальные файлы автоматически объединяются по этим правилам.

### Основные возможности

- ✅ Объединение множества Excel файлов (.xlsx) в один
- ✅ Гибкая настройка правил для каждого листа
- ✅ Поддержка разных строк заголовков в разных листах
- ✅ Интуитивный интерфейс с вкладками (3 простых шага)
- ✅ Drag & Drop для добавления файлов
- ✅ Предпросмотр результата перед сохранением
- ✅ Сохранение и загрузка профилей настроек
- ✅ Индикация прогресса обработки
- ✅ Кросс-платформенность (Windows, macOS, Linux)
- ✅ Один исполняемый файл без зависимостей
- ✅ Высокая производительность

## 🎯 Пример использования

**Задача:** Объединить квартальные отчеты о продажах в один файл

```
Входные файлы:
├── sales_Q1_2024.xlsx (базовый)
│   ├── Лист "Продажи" (заголовки в строке 1)
│   └── Лист "Возвраты" (заголовки в строке 2)
├── sales_Q2_2024.xlsx
├── sales_Q3_2024.xlsx
└── sales_Q4_2024.xlsx

Результат: sales_2024_full.xlsx
├── Лист "Продажи" (объединенные данные из 4 файлов)
└── Лист "Возвраты" (объединенные данные из 4 файлов)
```

## 🚀 Быстрый старт

### 1. Выберите базовый файл
Укажите Excel файл, который будет служить шаблоном

![Шаг 1: Выбор базового файла](https://raw.githubusercontent.com/DatKorso/Merge-excel/main/assets/github-imgs/tab1.png)

### 2. Настройте правила
Для каждого листа:
- Включите/выключите обработку
- Укажите номер строки с заголовками
- Просмотрите заголовки

![Шаг 2: Добавление файлов](https://raw.githubusercontent.com/DatKorso/Merge-excel/main/assets/github-imgs/tab2.png)

### 3. Добавьте файлы и объедините
Перетащите остальные файлы, нажмите "Объединить" и сохраните результат

![Шаг 3: Объединение файлов](https://raw.githubusercontent.com/DatKorso/Merge-excel/main/assets/github-imgs/tab3.png)

## 🛠 Технологический стек

- **Go 1.21+** - язык программирования
- **Fyne v2.4+** - GUI фреймворк (Material Design)
- **excelize v2.8+** - работа с Excel файлами

### Почему Go?

- ⚡ **Производительность:** В 5-10 раз быстрее обработка больших файлов
- 📦 **Развертывание:** Один исполняемый файл без зависимостей
- 💾 **Размер:** 30-50 MB (против 100-150 MB в Python)
- 🔧 **Разработка:** Простой код, быстрая компиляция
- 🚀 **Параллелизм:** Нативная поддержка goroutines

## 📦 Установка

### Готовое приложение (Рекомендуется)

Скачайте готовый исполняемый файл для вашей ОС:
- **Windows:** `excel-merger-windows.exe` (~35 MB)
- **macOS:** `excel-merger-macos` (~40 MB)  
- **Linux:** `excel-merger-linux` (~35 MB)

Просто запустите файл - никаких дополнительных установок не требуется!

### Из исходников (для разработчиков)

```bash
# Установите Go 1.21+ с https://go.dev/dl/

# Клонируйте репозиторий
git clone https://github.com/yourusername/merge-excel.git
cd merge-excel

# Установите зависимости
go mod download

# Запустите приложение
go run cmd/excel-merger/main.go

# Или соберите
go build -o excel-merger ./cmd/excel-merger
./excel-merger
```

## 📖 Документация

Полная документация находится в папке `docs/`:

- [**project-plan.md**](docs/project-plan.md) - План разработки проекта
- [**tech-stack.md**](docs/tech-stack.md) - Технологический стек и обоснование выбора
- [**architecture.md**](docs/architecture.md) - Архитектура приложения
- [**merge-algorithm.md**](docs/merge-algorithm.md) - Детальный алгоритм объединения
- [**user-guide.md**](docs/user-guide.md) - Руководство пользователя

## 🏗 Структура проекта

```
Merge-excel/
├── cmd/
│   └── excel-merger/
│       └── main.go           # Точка входа
├── internal/
│   ├── gui/                  # Интерфейс (Fyne)
│   │   ├── app.go
│   │   ├── base_file_tab.go
│   │   ├── file_list_tab.go
│   │   └── merge_tab.go
│   ├── core/                 # Бизнес-логика
│   │   ├── analyzer.go
│   │   ├── merger.go
│   │   └── config.go
│   ├── excel/                # Работа с Excel
│   │   ├── reader.go
│   │   └── writer.go
│   └── config/               # Управление профилями
│       └── manager.go
├── docs/                     # Документация
├── go.mod
└── README.md
```

## 🧪 Тестирование

```bash
# Запустить все тесты
go test ./...

# С покрытием кода
go test -cover ./...

# Подробный вывод
go test -v ./...

# Бенчмарки
go test -bench=. ./...

# Тесты конкретного пакета
go test ./internal/core
```

## 🔨 Сборка

### Подготовка окружения

#### Требования
- **Go 1.21+** - [Скачать и установить](https://go.dev/dl/)
- **Git** - для клонирования репозитория

#### Установка зависимостей

```bash
# Клонируйте репозиторий
git clone https://github.com/DatKorso/Merge-excel.git
cd Merge-excel

# Установите зависимости Go
go mod download
```

### Использование Makefile (рекомендуется)

Проект включает Makefile для упрощения сборки:

```bash
# Сборка приложения (оптимизированная, без предупреждений)
make build

# Быстрая сборка для разработки
make build-dev

# Сборка и запуск
make run

# Очистка
make clean

# Показать все доступные команды
make help
```

### Локальная сборка (для текущей ОС)

```bash
# Простая сборка
go build -o excel-merger ./cmd/excel-merger

# С оптимизацией размера и подавлением предупреждений (рекомендуется для macOS)
go build -ldflags="-s -w -extldflags '-Wl,-w'" -o excel-merger ./cmd/excel-merger

# Запуск
./excel-merger
```

### Кросс-платформенная сборка с Fyne CLI (рекомендуется)

Fyne CLI создает полноценные нативные приложения с иконками и правильным оформлением для каждой ОС.

#### Установка Fyne CLI

```bash
go install fyne.io/fyne/v2/cmd/fyne@latest
```

#### Сборка для macOS (из macOS)

```bash
# Перейдите в директорию с main.go
cd cmd/excel-merger

# Соберите приложение
~/go/bin/fyne package -os darwin -icon ../../assets/icon.png -release

# Результат: excel-merger.app (готовый app bundle)
```

#### Сборка для Windows (из macOS)

**Требуется установка MinGW:**

```bash
# Установите MinGW через Homebrew
brew install mingw-w64
```

**Сборка:**

```bash
# Перейдите в директорию с main.go
cd cmd/excel-merger

# Соберите приложение
CC=x86_64-w64-mingw32-gcc CGO_ENABLED=1 GOOS=windows GOARCH=amd64 \
  ~/go/bin/fyne package -os windows -icon ../../assets/icon.png -release -appID com.github.excel-merger

# Результат: excel-merger.exe (~25 MB)
```

#### Сборка для Linux

```bash
# Перейдите в директорию с main.go
cd cmd/excel-merger

# Соберите приложение
~/go/bin/fyne package -os linux -icon ../../assets/icon.png -release

# Результат: готовый исполняемый файл для Linux
```

### Сборка всех платформ одной командой

Создайте скрипт `build-all.sh` в корне проекта:

```bash
#!/bin/bash
set -e

echo "🔨 Сборка Excel Merger для всех платформ..."

# Переход в директорию с main.go
cd cmd/excel-merger

# macOS
echo "📦 Сборка для macOS..."
~/go/bin/fyne package -os darwin -icon ../../assets/icon.png -release
echo "✅ macOS: excel-merger.app"

# Windows
echo "📦 Сборка для Windows..."
CC=x86_64-w64-mingw32-gcc CGO_ENABLED=1 GOOS=windows GOARCH=amd64 \
  ~/go/bin/fyne package -os windows -icon ../../assets/icon.png -release -appID com.github.excel-merger
echo "✅ Windows: excel-merger.exe"

# Linux
echo "📦 Сборка для Linux..."
~/go/bin/fyne package -os linux -icon ../../assets/icon.png -release
echo "✅ Linux: готово"

# Перемещение в корень проекта
cd ../..
mv cmd/excel-merger/excel-merger.app . 2>/dev/null || true
mv cmd/excel-merger/excel-merger.exe . 2>/dev/null || true
mv cmd/excel-merger/excel-merger . 2>/dev/null || true

echo "🎉 Сборка завершена! Файлы находятся в корне проекта."
```

Сделайте скрипт исполняемым и запустите:

```bash
chmod +x build-all.sh
./build-all.sh
```

### Альтернатива: простая кросс-компиляция Go

⚠️ **Внимание:** Создает исполняемые файлы без GUI-оформления и иконок. Используйте только для быстрого тестирования.

```bash
# Windows
GOOS=windows GOARCH=amd64 go build -o excel-merger.exe ./cmd/excel-merger

# macOS
GOOS=darwin GOARCH=amd64 go build -o excel-merger-macos ./cmd/excel-merger

# Linux
GOOS=linux GOARCH=amd64 go build -o excel-merger-linux ./cmd/excel-merger
```

### Проверка сборки

```bash
# Проверьте размер файлов
ls -lh excel-merger*

# Ожидаемые размеры:
# excel-merger.app - macOS app bundle
# excel-merger.exe - ~25 MB (Windows)
# excel-merger - ~20-30 MB (Linux)
```

### Решение проблем

#### "command not found: fyne"
```bash
# Убедитесь, что GOPATH/bin в PATH
export PATH=$PATH:~/go/bin
# Или используйте полный путь: ~/go/bin/fyne
```

#### "windows.h file not found" (при сборке для Windows)
```bash
# Установите MinGW
brew install mingw-w64
```

#### Ошибки при сборке на macOS
```bash
# Обновите Xcode Command Line Tools
xcode-select --install
```

## 📝 Требования

- Go 1.21 или выше (только для сборки из исходников)
- Операционные системы: Windows 10+, macOS 11+, Linux (Ubuntu 20.04+)
- Поддержка Excel файлов формата .xlsx (Excel 2007+)
- Для готовых исполняемых файлов никаких зависимостей не требуется!

## 🎨 Скриншоты

### Автоматические обновления
Приложение автоматически проверяет наличие новых версий при запуске

![Уведомление об обновлении](https://raw.githubusercontent.com/DatKorso/Merge-excel/main/assets/github-imgs/update-alert.png)

## 🗺 Roadmap

### Версия 0.1.0 (MVP)
- [x] Планирование и архитектура
- [x] Миграция на Go + Fyne
- [x] Реализация ядра (чтение, объединение, сохранение)
- [x] Базовый GUI с табами
- [x] Тестирование
- [x] Сборка для Windows/macOS/Linux
- [x] Сохранение/загрузка профилей настроек
- [x] Темная тема интерфейса (встроено в Fyne)

## 🎨 Преимущества Go версии над Python

| Характеристика | Python версия | Go версия |
|---------------|---------------|-----------|
| Размер приложения | 100-150 MB | 30-50 MB |
| Скорость обработки | Базовая | 5-10x быстрее |
| Зависимости | Требуется Python runtime | Нет зависимостей |
| Запуск | ~2-3 секунды | <1 секунда |
| Память | ~150-200 MB | ~50-80 MB |
| Кросс-компиляция | Сложная (PyInstaller) | Простая (встроенная) |
| Распространение | Установщик ~150 MB | Один файл ~35 MB |

## 🤝 Вклад в проект

Мы приветствуем ваш вклад! Если вы хотите помочь:

1. Fork проекта
2. Создайте feature-ветку (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 👤 Автор

**Ваше имя**
- GitHub: [@yourusername](https://github.com/yourusername)

## 🙏 Благодарности

- Fyne за отличный GUI фреймворк
- excelize за мощную работу с Excel
- Go team за прекрасный язык программирования

---

**Статус проекта:** 🚧 В разработке

**Последнее обновление:** [last release](https://github.com/DatKorso/Merge-excel/releases/latest)

**Язык:** Go 1.21+
